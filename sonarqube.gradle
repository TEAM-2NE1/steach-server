//sonarqube
sonarqube {
    properties {
        property "sonar.projectName", "local steach-server"
        property "sonar.projectKey", "local steach-server"
        property "sonar.host.url", "http://steach.ssafy.io:9000"
        property "sonar.token", "sqp_a9e67d030ca9317eb4086833d3ff9417292eedaa"
        property "sonar.language", "java"
        property 'sonar.projectVersion', '1.1.0-SNAPSHOT'
        property "sonar.sourceEncoding", "UTF-8"
        property 'sonar.coverage.jacoco.xmlReportPaths', jacocoXmlDir.map { it.file('jacocoTestReport.xml') }.get().asFile.path
//        property 'sonar.java.binaries', '${buildDir}/classes'
        property 'sonar.test.inclusions', '**/*Test.java'
        property 'sonar.exclusions', '**/test/**, **/Q*.java, **/*Doc*.java, **/resources/**'
        property "sonar.sources", "src/main/java"

        property "sonar.java.binaries", "${projectDir}/build/classes/java/main"
        property 'sonar.coverage.jacoco.xmlReportPaths', jacocoXmlDir.map { it.file('jacocoTestReport.xml') }.get().asFile.path
    }
}
